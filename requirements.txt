# Methodological environment target -- HISTORICAL CLAIM, as inherited from the
# project brief's own summary of Ohashi et al.: Python 3.10.13, "TensorFlow
# 2.10.10." This exact TensorFlow version does NOT exist on PyPI -- the 2.10.x
# line only ever shipped 2.10.0 and 2.10.1 (confirmed 2026-08-14 against PyPI's
# release metadata; see docs/training_environment.md Section 2). "2.10.10" is
# almost certainly a transcription error somewhere upstream of this project;
# this is NOT a claim that Ohashi's paper itself says "2.10.1" -- the paper was
# never obtained (see docs/research_decisions.md), so the original brief's exact
# claim is preserved here as history, not silently corrected.
#
# VERIFIED PROJECT ENVIRONMENT (2026-08-14, docs/training_environment.md):
# tensorflow==2.10.1 -- the closest real release to the brief's claimed
# "2.10.10" -- is installed and verified working in a SEPARATE, ISOLATED
# Python 3.10.20 environment (C:\Users\surya\.venvs\ct-iqa-tf210, outside this
# repository). It is NOT installed in the environment this file governs.
#
# The CURRENT local development/test environment (governed by this file) is
# Python 3.13 (verified under both Anaconda 3.13.9 and, as of the 2026-08-14
# repository audit, the Microsoft Store CPython 3.13.14 build) -- fine for
# everything below the "deep learning" section, which is all this repository
# needs through the production-pipeline phase. TensorFlow is NOT installed
# here and must not be -- see docs/training_environment.md Section 7
# ("Isolation guarantee") for why training-environment provisioning
# deliberately stays out of this file and this Python installation. The
# RadImageNet ResNet50 checkpoint has been obtained (docs/training_environment.md
# Section 5) but is stored outside Git (data/external/radimagenet/, gitignored),
# not referenced by anything this file installs.
#
# Audited 2026-08-14 against actual `import` statements across src/, scripts/,
# tests/ -- every package below is imported somewhere in the codebase. Removed
# (were unused anywhere in the repo): pandas, scikit-learn, scikit-image. Do not
# re-add a dependency speculatively; add it in the same change that starts using it.

# --- core (installed, verified working in this environment) ---
numpy>=1.26
scipy>=1.11
pillow>=10.0
PyYAML>=6.0
pyarrow>=14.0
matplotlib>=3.8
jupyter>=1.0
pytest>=7.4

# --- deep learning: NOT installed here, NOT needed in this environment ---
# TensorFlow lives only in the separate isolated training environment
# (docs/training_environment.md) -- deliberately never added to this file.
# See docs/training_environment.md Sections 1-2 for the verified Python
# 3.10.20 / TensorFlow 2.10.1 combination and why "2.10.10" (this project's
# historically inherited claim) does not exist as an installable release.

# --- VIF (Visual Information Fidelity) ---
# Ohashi et al. computed VIF in MATLAB R2024a (Section 12 of the project brief) --
# there is no single canonical, universally-adopted Python VIF package to pin
# here, and inventing one would misrepresent what this project actually uses.
# This project implements VIFp (pixel-domain) from scratch in
# src/ct_iqa/vif/metric.py, following Sheikh & Bovik's published formulation.
# It depends only on numpy/scipy above -- no separate VIF package is installed.
# See docs/deviations_from_ohashi.md (DEV-03) for the numerical-validation
# status of this implementation.
#
# Decision A-15 (docs/research_decisions.md): Ohashi's citation actually
# points to Sheikh & Bovik's FULL wavelet-domain/GSM VIF, not VIFp. That
# formulation is implemented in src/ct_iqa/vif/wavelet.py (vif_wavelet()),
# built on the steerable-pyramid decomposition below. VIFp is kept, unchanged,
# as a separate variant -- see ct_iqa.vif.labeling.compute_vif.
pyrtools==1.0.10   # steerable-pyramid decomposition; installed and verified in
                   # this environment (Anaconda Python 3.13.9, Windows) on 2026-08-13.
                   # See docs/vif_implementation.md for API usage and version pin rationale.
