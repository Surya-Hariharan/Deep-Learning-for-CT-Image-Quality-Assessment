# Methodological environment target (Ohashi et al., as reported): Python 3.10.13,
# TensorFlow 2.10.10. This project's TRAINING code (once implemented) should run
# under that environment for fidelity to the original methodology.
#
# The CURRENT local development environment is Anaconda Python 3.13.9 -- fine for
# everything below the "deep learning" section, which is all this repository
# initialization phase needs. TensorFlow is NOT yet installed; do not install it
# until training is explicitly authorized (see docs/research_decisions.md, U-M08:
# the RadImageNet ResNet50 checkpoint itself is also not yet obtained).

# --- core (installed, verified working in this environment) ---
numpy>=1.26
scipy>=1.11
pandas>=2.0
pillow>=10.0
PyYAML>=6.0
pyarrow>=14.0
scikit-learn>=1.3
scikit-image>=0.22
matplotlib>=3.8
jupyter>=1.0
pytest>=7.4

# --- deep learning (Ohashi-faithful pin; NOT installed, NOT needed until
#     training is authorized) ---
# tensorflow==2.10.10   # methodological match to Ohashi et al.; requires Python <=3.10

# --- VIF (Visual Information Fidelity) ---
# Ohashi et al. computed VIF in MATLAB R2024a (Section 12 of the project brief) --
# there is no single canonical, universally-adopted Python VIF package to pin
# here, and inventing one would misrepresent what this project actually uses.
# This project implements VIFp (pixel-domain) from scratch in
# src/ct_iqa/vif/metric.py, following Sheikh & Bovik's published formulation.
# It depends only on numpy/scipy above -- no separate VIF package is installed.
# See docs/deviations_from_ohashi.md (DEV-03) for the numerical-validation
# status of this implementation.
#
# Decision A-15 (docs/research_decisions.md): Ohashi's citation actually
# points to Sheikh & Bovik's FULL wavelet-domain/GSM VIF, not VIFp. That
# formulation is implemented in src/ct_iqa/vif/wavelet.py (vif_wavelet()),
# built on the steerable-pyramid decomposition below. VIFp is kept, unchanged,
# as a separate variant -- see ct_iqa.vif.labeling.compute_vif.
pyrtools==1.0.10   # steerable-pyramid decomposition; installed and verified in
                   # this environment (Anaconda Python 3.13.9, Windows) on 2026-08-13.
                   # See docs/vif_implementation.md for API usage and version pin rationale.
