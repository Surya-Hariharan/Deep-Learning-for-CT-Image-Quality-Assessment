# Dependencies for the Quality-Aware Longitudinal Lung CT Follow-up Assistant.
#
# Groups kept separate on purpose. Group 1 is what the vendored NGP-Net
# implementation needs to run; group 2 is our own research code (currently
# empty -- nothing beyond group 1 is needed yet); groups 3-5 are optional
# extras not imported by anything under src/.
#
# This file must stay consistent with pyproject.toml's [project.dependencies]
# and [project.optional-dependencies]. Install with `pip install -e .` (or
# `pip install -e ".[train,dev,notebook]"` for the extras).


# =====================================================================
# 1. NGP-Net runtime dependencies
#    Source: the author's original requirements.txt (SimpleITK, numpy,
#    pytorch, setproctitle, pandas) plus scipy/scikit-image, which
#    preprocessing/volumes.py (migrated from utils/img_utils.py) imports
#    at module scope but the author's list omitted. setproctitle is
#    dropped entirely: it was only used by the author's CLI entry point
#    (utils/config.py) to rename the OS process, which this project's
#    ngpnet/config.py replaces without that side effect.
# =====================================================================
torch>=2.0
numpy>=1.24
SimpleITK>=2.2          # NIfTI reading, in preprocessing/volumes.py:load_as_array
scikit-image>=0.21      # imported at module scope by preprocessing/volumes.py
scipy>=1.11             # ndimage, in preprocessing/volumes.py
pandas>=2.0              # common/logging.py:LogWriter, data/png metadata


# =====================================================================
# 2. Our research dependencies
#    (none beyond group 1 today -- quality/uncertainty/fusion/reporting
#    are unimplemented; see their package docstrings)
# =====================================================================


# =====================================================================
# 3. Training-only -- install with:  pip install -e ".[train]"
# =====================================================================
# monai>=1.3            # WarmupCosineSchedule, in training/trainer.py:build_optimizer


# =====================================================================
# 4. Development -- install with:  pip install -e ".[dev]"
# =====================================================================
# pytest>=7.4


# =====================================================================
# 5. Notebook -- install with:  pip install -e ".[notebook]"
#    Needed only to run/regenerate notebooks/ (see notebooks/README.md).
# =====================================================================
# matplotlib>=3.8
# ipykernel>=6.29
# nbformat>=5.10
# nbclient>=0.10
# nbconvert>=7.16
