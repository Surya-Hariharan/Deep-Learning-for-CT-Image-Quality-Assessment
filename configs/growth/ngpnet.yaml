# NGP-Net replication (Component A) - SPECIFICATION STUB.
#
# This file deliberately contains almost nothing but nulls. NGP-Net is a
# REPLICATION target: every field here must be filled from the actual paper or
# the official repository, not from plausible reconstruction. A null below means
# "not yet read from the source", never "pick something sensible".
#
# Source availability check (2026-08-13): no NGP-Net paper PDF and no NGP-Net
# source repository is present anywhere in this project. Nothing here can be
# filled in until one of them is obtained.

name: ngpnet_replication
status: "BLOCKED - source paper and reference implementation not available locally"

dataset: png                      # see configs/datasets/png.yaml (also not downloaded)

input:
  representation: null            # [UNKNOWN] 2D slice / 2.5D / 3D patch
  patch_size: null                # [UNKNOWN]
  n_timepoints_consumed: null     # [UNKNOWN]
  interval_encoding: null         # [UNKNOWN] how irregular follow-up intervals enter the model
  intensity_window: null          # [UNKNOWN]
  resampled_spacing: null         # [UNKNOWN]

architecture:
  backbone: null                  # [UNKNOWN]
  temporal_module: null           # [UNKNOWN]
  parameter_count: null           # [UNKNOWN] "lightweight" is not a specification
  output_head: null               # [UNKNOWN] regression of size vs classification of growth

training:
  loss: null                      # [UNKNOWN]
  optimizer: null                 # [UNKNOWN]
  learning_rate: null             # [UNKNOWN]
  batch_size: null                # [UNKNOWN]
  epochs: null                    # [UNKNOWN]
  augmentation: null              # [UNKNOWN]
  seed_from: "configs/paths.yaml:random_seed"

splits:
  unit: patient                   # [PROJECT REQUIREMENT] non-negotiable regardless of the paper
  ratios: null                    # [UNKNOWN]
  cross_validation: null          # [UNKNOWN]

evaluation:
  metrics: null                   # [UNKNOWN]
  reported_baseline: null         # [UNKNOWN] needed to judge whether the replication succeeded
