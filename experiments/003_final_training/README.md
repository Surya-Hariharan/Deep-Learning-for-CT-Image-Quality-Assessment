# Experiment 003: Final Training

**Status: training AND independent final test-set evaluation both completed (2026-09-13).**

This README separates **TRAINING** (below, through "Next step") from the
**FINAL TEST EVALUATION** (a distinct section further down) -- training loss
and test error are measured on different data and must not be confused.

---

## TRAINING

## Objective

Final Ohashi-style RadImageNet ResNet50 CT-IQA model for this LDCT-IQAC
adaptation, using the learning rate selected by experiment 002
(**LR = 1e-3**, see `experiments/002_learning_rate_search/README.md`), trained
on ALL 1000 labeled LDCT-IQAC training images. This is an Ohashi-style model
adapted to LDCT-IQAC -- not an exact reproduction of the original paper's
experiment (see docs/replication/dataset_adaptation.md).

## Dataset

- Training samples used: 1000 (ALL labeled LDCT-IQAC training images)
- Validation samples used: 0 -- **IMPLEMENTATION DECISION**: experiment 002 already used the
  900/100 split to select the learning rate; holding out validation data again here would only
  reduce the final model's training data with no remaining selection to justify it. Not a paper fact.
- Test samples used: 0 -- the 300-image test set was never loaded in this experiment.

## Model

`OhashiResNet50` (`src/ct_iqa/models/`): RadImageNet-pretrained ResNet50 backbone
(PAPER FACT: ResNet50, RadImageNet pretraining) -> Global Average Pooling -> Dropout(0.5)
(IMPLEMENTATION DECISION: exact probability not specified by the paper) -> Linear(2048,1) ->
Sigmoid. No architectural change from experiments 001/002.

**RadImageNet initialization: verified.** RadImageNet weights loaded from weights/pretrained/radimagenet/resnet50/radimagenet_resnet50_backbone.pt: 265 matched, 53 missing, 0 unexpected. 53 num_batches_tracked counters left at 0 (expected -- not learned parameters).

## Preprocessing / target

OUR IMPLEMENTATION, unchanged from experiments 001/002: `LDCT-IQAC image [0,1] -> center crop
224x224 (PAPER FACT) -> [-1,1] normalization (IMPLEMENTATION DECISION) -> grayscale replicated to
3 channels inside the model (IMPLEMENTATION DECISION) -> ResNet50`. Target:
raw score [0,4] -> normalize_score -> [0,1] (Sigmoid-compatible).

## Configuration

| | |
|---|---|
| optimizer | Adam |
| loss | MSE (normalized [0,1] target space) |
| learning rate | 0.001 (selected by experiment 002) |
| batch size | 64 |
| epochs | 30 (completed, no early stopping, no validation-based stopping) |
| dropout | 0.5 |
| seed | 42 |

## Environment

Python 3.10.20, PyTorch 2.5.1+cu121, CUDA available: True, GPU: NVIDIA GeForce RTX 4060 Laptop GPU, device=cuda.

## Actual measured results

**These are real, measured numbers from this run -- no validation metric exists for this run
(no validation split), and no test metric was computed (test set untouched).**

- Epochs completed: **30/30**, no NaN/Inf loss at any epoch
  (checked every epoch during training; the run would have stopped immediately and raised if one occurred).
- Final (epoch 30) training loss: **0.001783**
- Training loss trajectory: started at 0.023430, ended at 0.001783.

## Checkpoint

`checkpoint/final.pt` (gitignored) -- **the epoch-30 (final) model, not a
validation-selected best checkpoint** (none exists for this run). Contains `model_state_dict`,
`optimizer_state_dict`, `epoch` (29, 0-indexed), the resolved `config`, `seed`
(42), and `checkpoint_type="final_epoch"`. **Independently reloaded in a fresh model
instance/process** and verified to produce finite, [0,1]-bounded predictions on a real training batch.

## Unexpected events

None. No NaN/Inf loss, no collapsed predictions, gradients present throughout.

## Test set status

**TEST SET NOT USED DURING FINAL TRAINING.** No test image, label, DataLoader, or metric was
constructed or computed anywhere in this experiment.

## What this experiment does NOT show

Test-set performance was not evaluated here. The next step is an independent evaluation of
`checkpoint/final.pt` against the untouched 300-image LDCT-IQAC test set. This experiment also
does not reproduce the original Ohashi paper's numerical results -- different dataset, different
label semantics (see docs/replication/dataset_adaptation.md) -- and no such claim is made here.

## Next step (as of training completion)

FINAL INDEPENDENT TEST-SET EVALUATION, using `experiments/003_final_training/checkpoint/final.pt`
against the untouched 300-image LDCT-IQAC test set. Not performed by this experiment -- see below.

---

## FINAL TEST EVALUATION (independent, 2026-09-13)

Produced by `notebooks/04_evaluation/01_test_set_evaluation.ipynb`
(generated by `tools/build_evaluation_notebook.py`), which loaded
`checkpoint/final.pt` into a **fresh** `OhashiResNet50` instance in a clean
process -- no reuse of experiment 003's in-memory training model or any
previous evaluation model. **TEST SET USED ONLY FOR FINAL EVALUATION** --
no test data, metric, or result was used to select the checkpoint, the
learning rate, any hyperparameter, or any calibration parameter. This is
the same untouched 300-image test set experiment 001 was evaluated
against.

**Evaluation-only, this run made no changes to training, the checkpoint,
the architecture, or preprocessing.**

- Checkpoint cross-check: `checkpoint_type="final_epoch"`, `epoch=29`
  (0-indexed, i.e. completed epoch 30) -- confirmed to be the final-epoch
  checkpoint, not a validation-selected one. The checkpoint's own recorded
  `dropout_p=0.5`, `in_channels=1`, `image_size=224` matched the current
  `configs/*.yaml` exactly.
- Test set integrity confirmed immediately before inference: **300 images,
  300 labels**, no missing/orphan labels, no duplicate filenames, no
  duplicate image content.
- All 300 inverse-transformed predictions fell within `[0, 4]` (Sigmoid
  head guarantees this by construction; no clipping applied or needed).

**RAW test metrics (original `[0,4]` score scale, n=300, no calibration):**

| PLCC | SROCC | KROCC | MSE | MAE | RMSE |
|---|---|---|---|---|---|
| 0.8490 | 0.8525 | 0.6611 | 0.4474 | 0.5195 | 0.6689 |

(MSE in normalized `[0,1]` target space, for reference: 0.027966.)

**Calibrated (5-parameter-logistic-mapped) test metrics: NOT computed.**
`ct_iqa.evaluation.calibration.five_parameter_logistic_fit` fits its
parameters directly against whichever `(y_pred, y_true)` pair it receives,
so fitting it on this test set would be circular. No validation-set-based
(non-test) calibration protocol exists in this project (and experiment 003
has no validation split to fit one against even if the protocol existed).
**Calibrated final-test evaluation was not performed because no non-test
calibration protocol has been established.** RAW results above are the
primary and only reported final result.

**Prediction distribution vs. ground truth (raw `[0,4]` scale):**

| | min | max | mean | median | std |
|---|---|---|---|---|---|
| ground truth | 0.000 | 4.000 | 2.131 | 2.167 | 1.087 |
| prediction | 0.024 | 3.689 | 1.791 | 1.752 | 0.878 |

- **Systematic underprediction observed**: mean prediction is **-0.3396**
  below mean ground truth (0-4 scale). This is the *opposite direction* of
  experiment 001's bias (+0.305, overprediction) -- reported as an observed
  characteristic of this specific final model, not corrected or hidden.
- Prediction std (0.878) is somewhat below ground-truth std (1.087) --
  ratio 0.81 -- a mild but real narrowing of the predicted range relative
  to experiment 001 (whose ratio was ~1.00), consistent with the residual
  pattern below (larger negative residuals at high ground-truth scores)
  rather than a full collapse toward the mean.
- No hard saturation at the score boundaries: predictions ranged
  `[0.024, 3.689]`; 3/300 predictions fell within 0.05 of `SCORE_MIN`, 0/300
  within 0.05 of `SCORE_MAX`.

**Residual analysis (`prediction - ground_truth`, `[0,4]` scale):**

| mean | median | std | min | max |
|---|---|---|---|---|
| -0.3396 | -0.2732 | 0.5763 | -2.2078 | +1.1197 |

By ground-truth quartile, the bias grows more negative as the true score
increases -- the model increasingly underpredicts high-quality images:

| ground-truth range | n | mean residual | std |
|---|---|---|---|
| [0.00, 1.17] | 82 | +0.108 | 0.370 |
| [1.17, 2.17] | 76 | -0.304 | 0.499 |
| [2.17, 3.00] | 77 | -0.410 | 0.451 |
| [3.00, 4.00] | 65 | -0.863 | 0.544 |

This is reported as an observed characteristic of this specific final
model/run, not corrected here.

- Full per-image predictions: `results/predictions/003_final_training_test_predictions.csv` (300 rows).
  Full metrics record: `results/metrics/003_final_training_test_metrics.json`.
  Diagnostic figures: `results/figures/003_final_training_test_{pred_vs_gt,residuals,score_distribution,error_vs_score}.png`.

## Experiment 001 vs. Experiment 003 (same untouched test set)

Both evaluated on the identical 300-image LDCT-IQAC test set -- same
dataset, same architecture, same preprocessing, **different training
protocol** (experiment 001: 900/1000 training images + validation-selected
checkpoint; experiment 003: all 1000/1000 training images + final-epoch
checkpoint, using the LR experiment 002 selected). See
`results/tables/001_vs_003_final_comparison.csv` for the machine-readable
version.

| Metric | Experiment 001 | Experiment 003 | Difference (003 - 001) |
|---|---|---|---|
| PLCC | 0.8804 | 0.8490 | -0.0314 |
| SROCC | 0.8793 | 0.8525 | -0.0268 |
| KROCC | 0.6952 | 0.6611 | -0.0341 |
| MSE | 0.3765 | 0.4474 | +0.0709 |
| MAE | 0.5024 | 0.5195 | +0.0171 |
| RMSE | 0.6136 | 0.6689 | +0.0553 |

**Observation, not overinterpretation:** on this test set, the final
(all-data, final-epoch) model performed empirically *worse* than the
900/1000 validation-selected baseline on every metric above, and its
prediction bias flipped direction (overprediction in experiment 001,
underprediction in experiment 003). No statistical significance test is
implemented in this project, so no claim of significant difference is made
-- these are the measured differences and nothing more. A plausible,
non-exhaustive explanation is that experiment 001's checkpoint was
explicitly selected by lowest validation loss (i.e., selected for
generalization), whereas experiment 003's final-epoch checkpoint was not
selected by any such criterion; this is offered as context, not as a
proven cause.

## Calibration status

Not performed for either experiment -- no validation-set-based (non-test)
5PL calibration protocol exists in this project's `src/ct_iqa/evaluation/`.
Raw, uncalibrated metrics are the final reported result for both
experiments 001 and 003.

## Test set contamination check

- Test set: 300 images, 300 labels (verified at evaluation time).
- Training completed before this evaluation.
- LR selection (experiment 002) completed before this evaluation, using
  only the validation split.
- Final checkpoint (`final.pt`) is an unconditional epoch-30 checkpoint --
  no validation- or test-based selection occurred in producing it.
- No calibration was fit on the test set.

**TEST SET USED ONLY FOR FINAL EVALUATION.**

## What this experiment does NOT show

This does not reproduce the original Ohashi paper's numerical results --
different dataset, different label semantics (see
`docs/replication/dataset_adaptation.md`) -- and no such claim is made
here. This is also not evidence that RadImageNet pretraining universally
outperforms ImageNet pretraining, that this architecture is
state-of-the-art, or of clinical usefulness -- it is evidence about this
specific model, on LDCT-IQAC, under this specific experimental protocol,
and nothing broader.

## Project status after this evaluation

The core experimental pipeline (001 baseline -> 002 LR search -> 003 final
training -> final test evaluation) is now **complete**. See
`docs/replication/final_results.md` for the full summary.

## Next step

No further training run is proposed. The next phase is analysis /
interpretation of the results already produced here (prediction analysis,
error analysis, visualization, final paper-vs-implementation
documentation, limitations, presentation/report preparation) -- not
another training run.
