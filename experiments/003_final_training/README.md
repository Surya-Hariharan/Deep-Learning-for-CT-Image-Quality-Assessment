# Experiment 003: final training

**Status: complete** — training and independent test-set evaluation both
finished (2026-09-13). This run trains on all labeled data using the
learning rate selected by experiment 002
(`experiments/002_learning_rate_search/`). See
[`docs/replication/final_results.md`](../../docs/replication/final_results.md)
for the full analysis and comparison against experiment 001.

Orchestrated by `notebooks/03_training/03_final_training.ipynb`
(generated by `tools/build_final_training_notebook.py`).

## Setup

- **Dataset:** all 1000 labeled LDCT-IQAC training images, no validation
  split (experiment 002 already used the 900/100 split to select the
  learning rate). The 300-image test set was not touched during training.
- **Model:** `OhashiResNet50`, unchanged from experiments 001/002.
  RadImageNet weights verified loaded (265/318 backbone keys matched, 0
  unexpected).
- **Config:** Adam, MSE loss, batch size 64, lr 1e-3 (selected by
  experiment 002), 30 epochs, seed 42. Full resolved config: `config.json`.

## Training result

- No validation-selected checkpoint exists for this run — the checkpoint
  is the unconditional final epoch (30).
- Final training loss: 0.001783 (started at 0.023430).
- No NaN/Inf loss at any epoch.

## Checkpoint

`checkpoint/final.pt` (gitignored) — the epoch-30 model, `checkpoint_type
= "final_epoch"` (not validation-selected, since no validation split
exists for this run). Independently reloaded and verified before
evaluation.

## Test-set result

Raw (uncalibrated) metrics on the untouched 300-image test set, the same
set experiment 001 was evaluated against, n=300:

| PLCC | SROCC | KROCC | MSE | MAE | RMSE |
|---|---|---|---|---|---|
| 0.8490 | 0.8525 | 0.6611 | 0.4474 | 0.5195 | 0.6689 |

This run underpredicts the mean ground-truth score by -0.34 on the
`[0,4]` scale (the opposite direction of experiment 001's bias), and
underperforms experiment 001 on every metric above. See
[`docs/replication/final_results.md`](../../docs/replication/final_results.md)
for the full residual/quartile breakdown and discussion of possible
causes (checkpoint selection method and training-set size are both
plausible, uncontrolled-for factors — not determined by this data alone).

Artifacts: `results/predictions/003_final_training_test_predictions.csv`,
`results/metrics/003_final_training_test_metrics.json`,
`results/figures/003_final_training_test_*.png`.

## Project status

The core experimental pipeline (001 baseline → 002 LR search → 003 final
training → test evaluation → analysis) is complete. No further training
run is planned.
